name: 'C++ CI'

env:
  PROJECT_NAME: hw1
  BUILD_PATH: build


on:
  push:
    paths:
      - hw1/**

jobs:
  build:
    runs-on: ubuntu-latest
    uses: actions/checkout@v2
    steps:
      - name: Install dependencies
        run: |
          sudo apt-get update && sudo apt-get install libgtest-dev libgmock-dev -y
      - name: Configure
        run: |
          mkdir -p $BUILD_PATH
          cmake -S./ -B./$BUILD_PATH -DCMAKE_BUILD_TYPE:STRING=Release
      - name: Build
        working-directory: ./$BUILD_PATH
        run: |
          pwd
          cmake --build .
      - name: Test
        working-directory: ./$BUILD_PATH
        run: |
          pwd
          ctest
